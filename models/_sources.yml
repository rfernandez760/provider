version: 2

sources:
  - name: nppes
    description: Raw provider data from NPPES, NUCC, and CMS
    database: insights_dbt
    schema: nppes
    tables:
      - name: npi
      - name: nucc_taxonomy
      - name: medicare_specialty_crosswalk
