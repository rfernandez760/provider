version: 2

sources:
  - name: nppes
    description: Raw provider data from NPPES, NUCC, and CMS
    database: operations
    schema: nppes
    tables:
      - name: npi
      - name: nucc_taxonomy
      - name: medicare_specialty_crosswalk
